{% extends 'base.html.twig' %}

{% block title %}Evenement{% endblock %}

{% block body %}
                    <h1 class="title_page">Evenement</h1>

                    <table class="table table-striped table-bordered js-datatable" id="tableEvents">
                        <tbody>
                            <tr>
                                <th>Description</th>
                                <td>{{ evenement.description }}</td>
                            </tr>
                            <tr>
                                <th>Date de début</th>
                                <td>{{ evenement.dateDebut ? evenement.dateDebut|date('Y-m-d') : '' }}</td>
                            </tr>
                            <tr>
                                <th>Date de fin</th>
                                <td>{{ evenement.dateFin ? evenement.dateFin|date('Y-m-d') : '' }}</td>
                            </tr>
                            <tr>
                                <th>Niveau et discipline</th>
                                <td>{{ evenement.idNivdisci }}</td>
                            </tr>
                        </tbody>
                    </table>


                            <a href="{{ path('evenement_index') }}"
                               class="btn btn-info btn-sm" title="Retour à la page evenement" data-toggle="tooltip">
                                <i class="fas fa-undo-alt"></i>
                            </a>

                            <a href="{{ path('evenement_edit', {'id': evenement.id}) }}"
                               class="btn btn-info btn-sm" title="Modifier l'evenement data-toggle="tooltip">
                                <i class="far fa-eye"></i>
                            </a>

                            <a href="{{ "now"|date("m/d/Y") }}"
                               class="btn btn-info btn-sm" title="Supprimer l'evenement" data-toggle="tooltip">
                                <i class="far fa-eye"></i>
                            </a>

    {{ "now"|date("m/d/Y") }}



                    "{{ path('evenement_delete', {'id': evenement.id}) }}"
                    {#<a href="{{ include('evenement/_delete_form.html.twig') }}"#}

                    <h1 class="title_page" >Liste des participants</h1>

                    <table class="table table-striped table-bordered js-datatable" id="tableEvents">
                        <thead>
                        <tr>
                            <th>Participants</th>
                            <th>actions</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for participation in participations %}
                            <tr>
                                <td>{{ participation.idUser.nom }}</td>
                                <td>
                                    <a href="{{ path('participation_show', {'id': participation.id}) }}">show</a>
                                    <a href="{{ path('participation_edit', {'id': participation.id}) }}">edit</a>
                                </td>
                            </tr>
                        {% else %}
                            <tr>
                                <td colspan="3">no records found</td>
                            </tr>
                        {% endfor %}
                        
                        </tbody>
                    </table>
                {% endblock %}
