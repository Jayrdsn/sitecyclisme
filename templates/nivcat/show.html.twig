{% extends 'base.html.twig' %}

{% block title %}Nivcat{% endblock %}

{% block body %}
    <h1>Nivcat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nivcat.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('nivcat_index') }}">back to list</a>

    <a href="{{ path('nivcat_edit', {'id': nivcat.id}) }}">edit</a>

    {{ include('nivcat/_delete_form.html.twig') }}
{% endblock %}
