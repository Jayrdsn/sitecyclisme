{% extends 'base.html.twig' %}

{% block title %}Nivdisci{% endblock %}

{% block body %}
    <h1>Nivdisci</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nivdisci.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('nivdisci_index') }}">back to list</a>

    <a href="{{ path('nivdisci_edit', {'id': nivdisci.id}) }}">edit</a>

    {{ include('nivdisci/_delete_form.html.twig') }}
{% endblock %}
